name: Publish Container

on:
  push:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/gw31415/ama-home-manager

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build container image
        run: nix build .#packages.x86_64-linux.dockerImage

      - name: Load image into Docker
        run: docker load < result

      - name: Materialize mutable container state
        env:
          REF_NAME: ${{ github.ref_name }}
          REF_TYPE: ${{ github.ref_type }}
        run: |
          set -eu

          branch=main
          if [ "${REF_TYPE}" = "branch" ]; then
            branch="${REF_NAME}"
          fi

          docker run \
            --name ama-home-manager-bootstrap \
            --volume "${GITHUB_WORKSPACE}:/work:ro" \
            --env HOME_MANAGER_GIT_BRANCH="${branch}" \
            ama-home-manager:latest \
            /bin/sh /work/container-post-build.sh /work

          docker commit ama-home-manager-bootstrap ama-home-manager:latest
          docker rm ama-home-manager-bootstrap

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login "${REGISTRY}" -u "${{ github.actor }}" --password-stdin

      - name: Tag image
        env:
          REF_NAME: ${{ github.ref_name }}
          REF_TYPE: ${{ github.ref_type }}
          SHA: ${{ github.sha }}
        run: |
          set -eu
          sanitize_ref() {
            printf '%s' "$1" | tr '/:@' '-' | tr -cd '[:alnum:]._-'
          }

          docker tag ama-home-manager:latest "${IMAGE_NAME}:sha-${SHA}"

          if [ "${REF_TYPE}" = "branch" ]; then
            docker tag ama-home-manager:latest "${IMAGE_NAME}:$(sanitize_ref "${REF_NAME}")"
          elif [ "${REF_TYPE}" = "tag" ]; then
            docker tag ama-home-manager:latest "${IMAGE_NAME}:$(sanitize_ref "${REF_NAME}")"
          fi

          if [ "${REF_NAME}" = "main" ]; then
            docker tag ama-home-manager:latest "${IMAGE_NAME}:latest"
          fi

      - name: Push image
        env:
          REF_NAME: ${{ github.ref_name }}
          REF_TYPE: ${{ github.ref_type }}
          SHA: ${{ github.sha }}
        run: |
          set -eu
          sanitize_ref() {
            printf '%s' "$1" | tr '/:@' '-' | tr -cd '[:alnum:]._-'
          }

          docker push "${IMAGE_NAME}:sha-${SHA}"

          if [ "${REF_TYPE}" = "branch" ] || [ "${REF_TYPE}" = "tag" ]; then
            docker push "${IMAGE_NAME}:$(sanitize_ref "${REF_NAME}")"
          fi

          if [ "${REF_NAME}" = "main" ]; then
            docker push "${IMAGE_NAME}:latest"
          fi
